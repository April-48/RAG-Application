# Frontend (Vite) environment template
# Copy to `frontend/.env`. Vite only loads env files from the frontend directory.
# Never commit `frontend/.env`.

# Middleware API base URL (used by src/api/client.ts at dev/build time)
VITE_API_BASE_URL=http://localhost:8000
